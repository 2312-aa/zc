"""
llm_emb_config.py — LLM/Embedding模型配置、阈值参数
从环境变量统一读取，提供全局访问接口
Rerank复用Embedding模型，无需独立配置
"""
import os
from dotenv import load_dotenv

load_dotenv(override=True)


# ============================================================
# Ollama服务地址
# ============================================================
OLLAMA_BASE_URL = os.getenv("OLLAMA_BASE_URL", "http://127.0.0.1:11434")

# ============================================================
# 向量嵌入模型配置（同时用于Rerank重排）
# ============================================================
OLLAMA_EMBED_MODEL = os.getenv("OLLAMA_EMBED_MODEL", "nomic-embed-text")

# ============================================================
# 对话大模型配置
# ============================================================
OLLAMA_LLM_MODEL = os.getenv("OLLAMA_LLM_MODEL", "qwen2.5:7b")

# ============================================================
# 模型参数
# ============================================================
LLM_TEMPERATURE = 0.1          # 生成温度，越低越确定
LLM_NUM_CTX = 4096             # 上下文窗口长度
EMBED_BATCH_SIZE = 32          # 批量向量化批次大小
